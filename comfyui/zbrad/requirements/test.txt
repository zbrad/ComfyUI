# Packages used only for testing, kept apart from custom-nodes.txt so a venv that
# never runs the deploy gate can skip them. Exact versions from a working venv
# (geo-a-node-gb10-1's), verified on geo-a-node-gb10-2 on 2026-09-21.
#
#   .venv/bin/python -m pip install --no-deps -r comfyui/zbrad/requirements/test.txt
#
# scripts/test-and-publish.sh needs:
#   unit tests (tests-unit/requirements.txt): pytest, pytest-aiohttp, pytest-asyncio.
#     Its fourth entry, websocket-client, is in custom-nodes.txt because the
#     resource watcher needs it at runtime too.
#   integration test (comfyui-test-integrations): playwright, mcp.
#     Playwright also needs a browser download: .venv/bin/python -m playwright install chromium

# test tooling
mcp==2.2.0
playwright==1.63.0
pytest==9.1.1
pytest-aiohttp==1.1.1
pytest-asyncio==1.4.0

# their dependencies
httpcore2==2.13.0
httpx2==2.13.0
iniconfig==2.3.0
mcp-types==2.2.0
opentelemetry-api==1.44.0
pluggy==1.6.0
pyee==13.0.1
python-multipart==0.0.32
sse-starlette==3.4.11
starlette==1.6.0
truststore==0.10.4
uvicorn==0.53.0
