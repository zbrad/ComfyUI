# Pins the GB10-tuned torch build so a later `pip install -r requirements*.txt`
# (torchvision/torchaudio hard-pin an exact torch version) can't silently
# swap it out for a generic PyPI/pytorch.org build. See
# ~/.claude/projects/github-com-zbrad-ComfyUI/memory/tuned_torch_venv_pip_constraint.md
torch==2.15.0+gb10.cu133.tuning.v29
