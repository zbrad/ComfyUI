# Pins the GB10-tuned torch build so a later `pip install -r requirements*.txt`
# (torchvision/torchaudio hard-pin an exact torch version) can't silently
# swap it out for a generic PyPI/pytorch.org build. Generated by
# gpu_tuned_protect_torch_pin (see https://github.com/zbrad/tuned-common) --
# rerun it whenever the tuned torch wheel is rebuilt/reinstalled to refresh
# this pin.
torch==2.15.0+gb10.cu134.tuning.v34
